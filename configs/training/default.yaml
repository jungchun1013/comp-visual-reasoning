epochs: 10
lr: 1.0e-4
weight_decay: 0.05
warmup_epochs: 2
grad_clip: 1.0
mixed_precision: bf16
log_every: 100
save_every: 5
eval_every_steps: null  # null = eval only at epoch end
resume: null  # path to checkpoint for resuming training
