# @package _global_
defaults:
  - override /model: dinov2_learned_text
  - override /data: clevr
  - override /task: decoder
  - override /training: default

data:
  val_batch_size: 512

task:
  decoder:
    num_layers: 1

training:
  epochs: 16

wandb:
  name: clevr_dinov2_learned_text_decoder1l
