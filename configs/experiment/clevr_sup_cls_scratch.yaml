# @package _global_
defaults:
  - override /model: steervit_sup
  - override /data: clevr
  - override /task: classification
  - override /training: default

data:
  val_batch_size: 512

task:
  num_answers: 28

wandb:
  name: clevr_sup_cls_scratch

training:
  epochs: 16
