# @package _global_
defaults:
  - override /model: mot
  - override /data: clevr
  - override /task: mot
  - override /training: default

data:
  val_batch_size: 128

training:
  epochs: 16
  lr: 3e-4
