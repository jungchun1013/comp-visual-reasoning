# @package _global_
defaults:
  - override /model: steervit_dino
  - override /data: gqa
  - override /task: classification
  - override /training: default

data:
  batch_size: 32
  val_batch_size: 256

task:
  num_answers: 1500

training:
  epochs: 20
